# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json
# 스택 3/3 — 화이트리스트(Azure Firewall). private 스택을 먼저 배포한 뒤 실행한다.
name: hol-ai-foundry-whitelist
metadata:
  template: lab-azure-hol-ai-services-whitelist@1.0.0
infra:
  provider: bicep
  path: .
  module: main
