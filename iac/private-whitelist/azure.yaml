# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json
# 시스템 3/3 — Private 망 + URL 화이트리스트. 이 디렉터리에서 `azd up` 을 실행한다. 다른 시스템 의존은 없다.
name: hol-ai-foundry-private-whitelist
metadata:
  template: lab-azure-hol-ai-services-private-whitelist@1.0.0
infra:
  provider: bicep
  path: .
  module: main
