# azd 환경 디렉터리. VM_ADMIN_PASSWORD 등 비밀 값이 .env로 저장되므로 절대 커밋하지 않는다.
.azure/
.env
*.env.local

# Bicep 빌드 산출물
*.bicep.json
main.json

# OS / 편집기
.DS_Store
Thumbs.db
.vscode/
.idea/

# Python 실습 코드
__pycache__/
*.pyc
.venv/
venv/

# prepackage 가 선택한 문서를 이 고정 이름으로 복사한다. 원본은 assets/ 에 그대로 있다.
python/assets/document.md
